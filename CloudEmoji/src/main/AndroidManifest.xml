<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="org.ktachibana.cloudemoji"
          android:versionCode="1"
          android:versionName="0.5">

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="18"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="org.ktachibana.cloudemoji.MainActivity"
            android:launchMode="singleTop"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="org.ktachibana.cloudemoji.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:icon="@android:color/transparent"
            android:parentActivityName="org.ktachibana.cloudemoji.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.ktachibana.cloudemoji.MainActivity"/>
        </activity>
    </application>

</manifest>
