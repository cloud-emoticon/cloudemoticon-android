<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- The main content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- The primary toolbar -->
        <include layout="@layout/view_primary_toolbar" />

        <!-- The secondary toolbar with background color and padding -->
        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="@dimen/secondaryToolbarMinHeight"
            android:background="@color/primary" >
            
            <FrameLayout
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:layout_marginLeft="@dimen/secondaryToolbarMargin"
                android:layout_marginRight="@dimen/secondaryToolbarMargin">

                <include layout="@layout/view_secondary_toolbar"/>

            </FrameLayout>

        </LinearLayout>

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_marginLeft="@dimen/secondaryToolbarMargin"
            android:layout_marginRight="@dimen/secondaryToolbarMargin"
            android:orientation="vertical">

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@android:color/darker_gray" />

            <!-- The mainContainer -->
            <FrameLayout
                android:id="@+id/mainContainer"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

        </LinearLayout>


    </LinearLayout>

    <!-- The leftDrawer -->
    <FrameLayout
        android:id="@+id/leftDrawer"
        android:layout_width="@dimen/drawerSize"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@android:color/background_light"
        android:fitsSystemWindows="true" />

</android.support.v4.widget.DrawerLayout>
